# Copy to .env and fill in. .env is gitignored.
#
# Get the token from the Cloudflare Zero Trust dashboard:
#   Networks > Tunnels > your tunnel > Configure > Install connector
#
# Treat it like a password. If it leaks, delete the tunnel and create a new
# one; rotating the token is not possible.
TUNNEL_TOKEN=
