# Copy to .env and fill in. .env is gitignored.
VW_DOMAIN=https://vault.example.com

# Generate with:  openssl rand -base64 48
# Leave EMPTY to keep the admin panel disabled, which is the normal state.
VW_ADMIN_TOKEN=
